
import 'package:flutter/material.dart';

class OfflineImageComponent extends StatelessWidget {
const OfflineImageComponent({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}