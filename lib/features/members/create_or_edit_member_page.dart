import 'package:flutter/material.dart';

class CreateOrEditMemberPage extends StatelessWidget {
const CreateOrEditMemberPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Or Edit'),
      ),
      body: Container(),
    );
  }
}