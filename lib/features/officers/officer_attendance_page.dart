import 'package:flutter/material.dart';

class OfficerAttendancePage extends StatelessWidget {
const OfficerAttendancePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}