import 'package:flutter/material.dart';

class OfficerHomePage extends StatelessWidget {
const OfficerHomePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(
    
      appBar: AppBar(
        title: const Text('Titl9e'),
      ),
      body: Container(),
    );
  }
}