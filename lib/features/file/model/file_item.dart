class FileItem {
  final String name;       // The name of the file
  final String extension;  // The file extension (e.g., 'pdf', 'docx')

  FileItem({
    required this.name,
    required this.extension,
  });
}
